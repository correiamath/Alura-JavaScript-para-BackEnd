const notas = [7, 7, 7];

notas.push(8); //adiciona o número 8 a lista de notas

console.log(notas);

const media = (notas[0] + notas[1] + notas[2] + notas[3]) / notas.length;

console.log(media);
