const pessoa = {
  nome: "Luma",
  profissao: "Engenheira",
};

pessoa.cel = 75992211221;
pessoa.nome = "Luma Silva";

console.log(pessoa);

//A constante não pode ser alterada, mas o objeto dentro da constante, sim.
